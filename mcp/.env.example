# CleanCook MCP server — local env (PRD §4.2).
# The service-role key BYPASSES all Row Level Security. Keep it server-side only,
# never commit a filled-in .env, and only run this server on a trusted machine.
NEXT_PUBLIC_SUPABASE_URL=https://uqidqjuzuakovlpofadb.supabase.co
SUPABASE_SERVICE_ROLE_KEY=
